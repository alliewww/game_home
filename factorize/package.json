{
  "name": "prime-factorization",
  "version": "1.0.9",
  "description": "",
  "main": "script.js",
  "scripts": {
    "update-version": "node update-version.js",
    "version": "npm run update-version && git add public/service-worker.js",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm version patch && npm run build && gh-pages -d dist -e factorize"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.13.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.0.5"
  }
}
